name: Go Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ["1.22.x", "1.26.x"]

    steps:
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go-version }}

      - name: Check out code
        uses: actions/checkout@v6

      - name: Get dependencies
        run: go get -v -t -d ./...

      - name: Build
        run: go build -v ./...

      - name: Run tests with race detector
        run: go test -race -v ./...

  perf-sanity:
    name: Perf Sanity
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.x"

      - name: Get dependencies
        run: go get -v -t -d ./...

      - name: Benchmark repo-wide validation
        run: go test -run=^$ -bench=BenchmarkCheckedInRepoValidation -benchmem -benchtime=1x ./internal/validation

      - name: Benchmark analyzer cold pass
        run: go test -run=^$ -bench=BenchmarkCheckedInRepoAnalyzerColdPass -benchmem -benchtime=1x ./internal/validation

      - name: Benchmark analyzer warm pass
        run: go test -run=^$ -bench=BenchmarkCheckedInRepoAnalyzerWarmPass -benchmem -benchtime=1x ./internal/validation

      - name: Benchmark module-plugin path
        run: go test -run=^$ -bench=BenchmarkCheckedInRepoModulePluginPath -benchmem -benchtime=1x ./plugin

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.x"

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.7.2

  golangci-module-plugin-smoke:
    name: Golangci Module Plugin Smoke
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.24.x"

      - name: Install pinned golangci-lint
        run: go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.7.2

      - name: Build custom golangci-lint binary
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: bash ./scripts/ci/build-custom-gcl.sh

      - name: Run module plugin smoke test
        run: bash ./scripts/ci/run-golangci-plugin-smoke.sh
